# Live Tests

Live tests hit external providers and are optional in local default runs.

Examples:
- live LLM calls
- live TTS calls
- live storage checks

Run explicitly when validating provider integration:

```bash
pnpm test:live
```
