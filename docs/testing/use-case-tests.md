# Use-Case Tests

Use-case tests validate business behavior without HTTP transport concerns.

## Guidelines

- mock only boundary dependencies
- assert typed failures and success outputs
- include authorization and error-path cases
